#return statement=#functions send values or objects back to caller

from audioop import mul
from unittest import result


def multiply(number1,number2):#num1 and 2 are arguments
    result=number1*number2
    return result
    

print(multiply(4,5))  #the values are given here so these are returned back to function or caller
